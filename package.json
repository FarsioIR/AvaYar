{
  "name": "ava-farsio-runtime",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Executable foundation for Ava, a Farsio product.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm run lint && npm test && npm run build && npm run smoke",
    "live:azure": "node scripts/live-azure-probe.mjs"
  }
}
